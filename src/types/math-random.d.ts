declare module 'maath/random/dist/maath-random.esm' {
    const inSphere: any; // ou substitua 'any' pelo tipo correto, se possível
    // Adicione outras declarações conforme necessário
    export { inSphere };
}
